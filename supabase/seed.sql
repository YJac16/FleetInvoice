-- Optional seed data for local development.
-- Create users via Supabase Auth first, then upsert profiles/drivers.

-- Example (replace UUIDs with real auth.users ids):
-- INSERT INTO public.profiles (id, email, full_name, role)
-- VALUES
--   ('00000000-0000-0000-0000-000000000001', 'admin@example.com', 'Office Admin', 'admin'),
--   ('00000000-0000-0000-0000-000000000002', 'driver@example.com', 'Alex Driver', 'driver')
-- ON CONFLICT (id) DO UPDATE
-- SET email = EXCLUDED.email,
--     full_name = EXCLUDED.full_name,
--     role = EXCLUDED.role;
